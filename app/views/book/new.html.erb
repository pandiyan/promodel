<h1>Add new book</h1>
<div #style= "margin:0 auto; background-color:#383838 ;width:1200px;height:500px;border:2px solid white">
<%= form_for @book, :url=>{:action => :create, :controller => :book}, :html => { :multipart => true } do |f| %>
 
  <% if @book.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@book.errors.count, "error") %> prohibited this movie from being saved:</h2>

      <ul>
      <% @book.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
<div class="field" >
<%= f.label :Title %> <br />
  <%= f.text_field :title %>
   </div>
   <div class="field" >
<%= f.label :Price %> <br />
  <%= f.text_field :price %>
   </div>
 <div class="field" >
<%= f.label :Subject %> <br />
<%= f.collection_select(:subject_id,@subjects,:id,:name) %></p>
  </div>
 <div class="area" >
<%= f.label :Description %> <br />
<%= f.text_area :description%>
  </div>
 <div class="field" >
 <%= f.label :photo, "Photo" %>
      <%= f.file_field :photo %>
  </div>
 <div class="actions">
<%= f.submit%>
 </div>
<%end%>
<%= link_to 'Back', {:action => 'list'} %>

